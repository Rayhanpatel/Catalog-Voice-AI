# Google Cloud project ID — must match the project where Vertex AI and Cloud TTS are enabled.
# The proxy reads this at server start via gcloud; the browser embeds it in API request paths.
# Run: gcloud config set project <your-project-id>
VITE_GOOGLE_CLOUD_PROJECT=wine-voice-explorer
